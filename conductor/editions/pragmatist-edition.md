# Pragmatist Edition  →  built as the *pragmatist* voice

> **Implemented as a voice.** Produced by the **pragmatist** voice —
> see `../../voices/pragmatist/VOICE.md`. Run after the spine is complete (GATE 6).

**Register:** Formula → when to use it → compact worked example → ~12 practice problems. Zero personality; just gets the job done.

**Inputs:** the finished spine chapters in `chapters/`.
**Outputs:** `voices/pragmatist/<same-filename>.md` — one per chapter, `chapters/` untouched.
**Preserves:** title (as `# Chapter X — Title`), all markdown comments
(incl. `<!-- → [TABLE: ...] -->`), any LLM exercises, any images.

See `../../voices/pragmatist/VOICE.md` for the full definition and the invoke block,
and `../../voices/README.md` for how voice rewrites work.
