# Command-Line Quick Reference

> **Edition stub.** Run only after the spine is complete (GATE 6 signed).
> Expand this into a full prompt when you decide to build it.

**What it is:** A one-page cheat sheet of the commands, prompts, and flags the book uses.

**When to use:** You want a printable/scannable reference for the book's CLI workflow.

**Inputs:** the finished spine chapters in `chapters/` (and `research/` where relevant).

**Outputs:** `editions/cli-quick-reference.md` — a scannable cheat sheet.

## Starter prompt

```
Build a one-page quick reference of every command, script, and prompt pattern the book uses (including this repo's scripts/ tools and conductor/ prompts). Group by task. For each: the exact invocation, what it does in 6 words, and the most useful flag/option. Keep it dense and scannable, not prose.
```
